on: push
#  schedule:
#    - cron: '0 8 * * *'
jobs:
  fetch_currencies:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - run: yarn
      - run: yarn start
        env:
          AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}